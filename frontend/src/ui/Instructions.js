import React from 'react';
import { withClassNames } from './utils';


const Instructions = (props) => {
  return (<div {...props} />);
}

export default withClassNames('instructions')(Instructions);
